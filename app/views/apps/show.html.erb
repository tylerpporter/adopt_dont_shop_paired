<section class="info">
  <p>Name: <%= @app.name %></p>
  <p>Address: <%= @app.address %></p>
  <p>City: <%= @app.city %></p>
  <p>State: <%= @app.state %></p>
  <p>Zip: <%= @app.zip %></p>
  <p>Phone Number: <%= @app.phone_number %></p>
  <p>Description: <%= @app.description %></p>
  <% @app.pets.each do |pet| %>
    <p>Pets to Adopt: </p>
    <section id = "pet-<%= "#{pet.id}" %>">
      <%= link_to "#{pet.name}", "/pets/#{pet.id}" %> |
      <%= link_to "Approve Application", "/pets/#{pet.id}/apps", method: :patch %>
    </section>
  <% end %>
  <br />
</section>
